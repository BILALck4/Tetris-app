image: docker:latest

services:
  - docker:dind

stages:
  - build
  - test
  - notify

# Build stage
build:
  stage: build 
  script:
    - docker buildx build -t bilalck/tetris-app:latest .
    - docker run  --rm bilalck4/tetris-app  
  artifacts:
    paths:
      - bin/  
# Test stage
test_job:
  stage: test
  image: bilalck4/tetris-app
  before_script:
    - apt-get update -y
    - apt-get install -y ant curl
  script:
    - ant test  # Run tests and generate reports
  artifacts:
    paths:
      - test-reports/  # Save test reports as artifacts

#deploy_job:
#  stage: deploy
#  script:
#    - echo "Deploying the application..."
#    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin 
#    - docker push bilalck4/tetris-app:latest
#  only:
#    - main 

notify:
  stage: notify
  script:
    - echo "Envoi de la notification à Discord..."
    - >
      curl -H "Content-Type: application/json" \
      -d '{"content": "🎉 Le pipeline GitLab a réussi ! Build & Déploiement de l'application **Tetris** ont été effectués avec succès !"}' \
      $DISCORD_WEBHOOK_URL
  when: always
