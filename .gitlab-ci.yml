image: docker:latest

services:
  - docker:dind

variables:
  IMAGE_NAME: tetris-app
  IMAGE_TAG: latest

stages:
  - build

build:
  stage: build
  tags:
    - dosi

  script:
    - docker build -t $IMAGE_NAME:$IMAGE_TAG .
    - docker run --rm $IMAGE_NAME:$IMAGE_TAG
